# 调研发现 - 壁纸应用 UI 重构

## 现状分析
- **壁纸卡片**：
  - 间距 (16vp) 过大，导致视觉松散。
  - 缺少信息区域（标题/元数据），由于图片颜色风格迥异，列表显得杂乱。
  - `ModernWallpaperCard` 结构过于简单，缺乏设计深度。
- **筛选框**：
  - 顶部导航栏与下拉菜单的样式不统一。
  - 配色冲突 (`#007DFF` vs `#2262db`)，视觉识别度低。
- **详情页**：
  - 遇到非全面屏比例图片时，顶部出现大面积纯黑，视觉断层严重。
  - `ImageFit.Contain` 模式下的空白区域未利用，缺乏氛围感。
  - 整体设计语言与首页的“现代感”脱节。

## 设计灵感
- **小红书 (Xiaohongshu)**：采用带有卡片容器的布局，强调精美的排版和清晰的信息层级。
- **HarmonyOS 设计规范**：利用模糊效果 (Blur)、圆角 (20vp+) 和系统 SymbolGlyph 增强原生感。

## 技术关键点
- 瀑布流 (`WaterFlow`) 间距调整为 8-12vp，增加内容密度。
- 统一使用 `ModernFilterChip` 作为底层筛选组件。
- 详情页引入动态背景（如壁纸的高斯模糊版或提取主色调渐变）。
