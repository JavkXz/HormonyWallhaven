# HarmonyOS Wallpaper App 优化与演进路线图 (Expert Edition)

**文档版本:** 3.5 (Architecture Refined)
**最后更新:** 2026-02-09
**状态:** Phase 2 (架构升级与体验精细化) 启动

---

## 🚀 核心架构演进 (New!)

### Phase 2.5: 响应式架构重构 (当前重点)
- [ ] **全局状态管理 V2**
  - 使用 `@ObservedV2` 和 `@Trace` 替代碎片化的状态传递。
  - 实现 `GlobalState` 中心，管理：网络状态、全局收藏 ID 列表、用户信息。
- [ ] **网络层迁移 (RCP)**
  - 从 `@ohos.net.http` 迁移至 `@hms.collaboration.rcp`。
  - 引入请求拦截器（Logging, Error Handling）。
- [ ] **“一多”适配基础**
  - 引入 `BreakpointType` 适配手机、折叠屏和平板。

---

## 🎯 任务拆解

### 2. 用户体验精细化 (UX Refinement)

#### 2.1 状态实时同步 🔄 **进行中**
- [x] **定义全局状态**：创建 `GlobalState` 类。
- [ ] **双向联动**：详情页收藏操作立即反映在首页和收藏页，无需重新请求数据库。
- [ ] **触感反馈增强**：为不同操作匹配更细腻的振动曲线。

#### 2.2 空状态与异常处理 🔄 **进行中**
- [ ] **断网感知**：集成 `netConn.registerNetConnCallback` 实时感知网络切换。
- [ ] **优雅降级**：无网时优先展示本地缓存/收藏壁纸。

---

## 🛠️ 技术深度优化 (Phase 3)

### 3.1 极致加载性能
- [ ] **预加载 2.0**：基于 `WaterFlow` 的 `onScrollIndex` 计算滑动速度，动态调整预加载深度。
- [ ] **并发图像处理**：利用 `TaskPool` 进行主色调提取，避免阻塞 UI 线程。
- [ ] **二级缓存方案**：
  - L1: 内存 PixelMap 缓存 (LRU)
  - L2: 沙箱文件系统缩略图缓存

### 3.2 交互动效
- [ ] **自定义转场**：利用 `geometryTransition` 优化大图预览打开动画。
- [ ] **沉浸式适配**：全量适配 API 12 的避让区域，实现 0 边框全屏体验。

---

## ✅ 实施记录

| 时间 | 版本 | 核心变更 |
|-----|------|---------|
| 2026-02-04 | v3.0 | 完成持久化 (Preferences/RDB) |
| 2026-02-09 | v3.5 | 引入状态管理 V2 架构建议，启动 RCP 调研 |

---

## 📊 性能目标 (Revised)

| 指标 | 目标 | 优化手段 |
|-----|------|---------|
| 收藏同步延迟 | < 16ms (1帧) | @ObservedV2 响应式同步 |
| 列表滑动帧率 | 120fps (支持设备) | LazyForEach + TaskPool 预处理 |
| 首屏网络耗时 | 减少 20% | RCP HTTP/3 开启 |

---
*Generated by Gemini CLI Agent - 2026-02-09*